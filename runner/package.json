{
  "name": "kimi-workflows",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Run Claude Code dynamic-workflow scripts against Kimi CLI instead of Claude subagents. The workflow DSL (agent/parallel/pipeline/phase/log/budget/args/workflow) is preserved verbatim; only agent() is rebound to a headless Kimi CLI prompt.",
  "bin": {
    "run-workflow": "bin/run-workflow.js",
    "fleet": "bin/fleet.js"
  },
  "license": "MIT",
  "scripts": {
    "handshake": "node test/handshake.js",
    "test": "node test/all.js",
    "example": "node bin/run-workflow.js ../examples/hello.workflow.js --frontier"
  },
  "engines": {
    "node": ">=18.17"
  }
}
